[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Verbose output
addopts = -v --tb=short

# Coverage (optional)
# addopts = --cov=. --cov-report=html --cov-report=term
